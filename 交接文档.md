# 怀瑾 · 项目交接文档(给 Trae)

> 最后更新:2026-07-14　|　状态:**已上线**。当骗子玩法已重写为「节拍剧情引擎(VN)」并加入输赢机制;跑过一轮 8 维度审计,高危已修。

---

## 0. 一句话

**怀瑾**是反诈"心理疫苗站":让你**亲手当一次骗子**去操控 AI 受害者,得手瞬间反转——"你骗到的是一个像你妈妈的人,这些招别人正用在你身上",建立可迁移的反诈免疫。名字出自《楚辞·九章·怀沙》"怀瑾握瑜"。
Slogan:**当所有系统都在保你的钱,怀瑾来保你的人。**

三大模块:**当骗子(接种)** · **可疑分析(判读)** · **心理疗愈(复原)**。

---

## 1. 线上地址 & 仓库(先记这个)

| 项 | 值 |
|---|---|
| 🔗 线上 | **https://huaijin.vercel.app**(Vercel,`main` 推送即自动部署) |
| 仓库 | github.com/**vibe-GIF/huaijin**,分支 **`main` = 源码** |
| 本地 | `npm install && npm run dev` → **localhost:5174** |
| 工作目录 | ⚠️ **`E:\trae ai\怀瑾`**(不是 `C:\Users\JYQ74\huaijin` 那个旧被骗版副本) |

---

## 2. 部署架构 & 密钥红线(务必看)

- **生产 = Vercel**。前端请求 `/api/llm`,由 **`api/llm.js`(Vercel serverless 函数)** 代理到智谱 GLM;密钥 = Vercel 环境变量 **`HJ_LLM_KEY`**(服务端,永不进仓库/前端包)。
- **本地 dev** = `vite.config.js` 的 `server.proxy` 把 `/api/llm` 转发到智谱,注入 `.env` 里的 `HJ_LLM_KEY`。`.env` 被 `.gitignore` 忽略。
- **base 路径条件化**(`vite.config.js`):dev / Vercel 构建 = `'/'`;GitHub Pages 构建 = `'/huaijin/'`(靠 `process.env.VERCEL` 判断)。
- ⚠️ `.github/workflows/deploy.yml`(推 gh-pages 的 Action)**目前是失败/弃用状态,别依赖它**。正式部署只走 Vercel。
- 🔒 **密钥红线(不可破)**:key 绝不写进 `src/` `dist/` 或任何被 git 跟踪的文件;只存在于 `.env`(本地)和 Vercel 环境变量(线上)。改代码时如需 LLM,一律走 `/api/llm`,不要在前端出现 key。

---

## 3. ★ 架构大变更:当骗子已改成「节拍剧情引擎(VN)」

这是本次最大的改动,Trae 接手前必须理解:

**数据(`src/data.js` 的 `STORY`)**
- 三个受害者 `mom / student / investor`,每人一条 `beats` 数组。三类拍:
  - `auto` 自动播放的一句(骗子或受害者),读者只看;
  - `skip` 时间跳跃分隔「—— 三天后 ——」;
  - `hinge` **关键时刻**:暂停,给 2–3 个"框架"选择 + 自由输入,玩家亲手动手;
  - `win` 收网结算拍。
- hinge 之后紧跟的 `（h数字 好）/（h数字 急）` 受害者台词 = 分支反应,由引擎按玩家选择的 `quality` 挑一条显示,另一条跳过。**这些 marker 台词自动播放阶段要跳过,别漏进 UI。**

**引擎(`src/Scammer.jsx` 的 `Play`)**
- **`cursorRef` 是推进真源**(不是 `cursor` state)——因为递归 `advance()` + StrictMode 会让 state 闭包 stale 导致卡死。`aliveRef` 也要在 effect setup 里重置为 `true`(StrictMode 双挂载会把它置 false)。
- **火候双表**:信任 `trust` + 戒心 `susp`(用 `trustRef/suspRef` 做真源)。技法参数在 `TECH_PARAMS`,阶段在 `STAGES`,化解怀疑技法在 `RESOLVE_TECHS`。到点用对招→信任涨;跳步/太急→戒心飙、受害者起疑。
- **可输(关键)**:
  - 收网(`resolveEnding`)要求 `信任 ≥ WIN_TRUST(70)` 且 `戒心 ≤ WIN_SUSP_CAP(62)`,否则 `loseFinal()`——她临门缩回"先问问家里人"→ 输。
  - 戒心 `≥ SUSP_WARN(70)` 给**一次**警告浮层(guard,退一步);再爆 `≥ SUSP_MAX(100)` → `onLose('spooked')` 被拉黑 → 输。
  - `LosePage` 是**反向教学**:"她逃掉,因为她做对了这几件事(被催/被要求瞒家人/升温太快时起疑)"。
- **反转(`Reveal`)**:只复述玩家在 hinge 上**亲手选/输入**的话(`movesRef`),盖技法印章;`victim.revealText`「像你妈妈/弟弟/同事」不变。
- 结局三态:**赢→反转** / **戒心爆→spooked 输** / **火候不够→final 输**。

---

## 4. 文件地图

| 文件 | 作用 |
|---|---|
| `src/App.jsx` | 首页 + hash 路由(`#/sandbox` 当骗子 / `#/lens` 可疑分析 / `#/heal` 疗愈 / `#/sandbox-old` 旧被骗版) |
| `src/Scammer.jsx` | ★ 当骗子:VN 引擎 Play + Reveal/Label/Radar/Card/**LosePage** |
| `src/data.js` | `STORY`(剧情)、`VICTIMS`(含 suspLines/objection/revealText)、`TECH_PARAMS/STAGES/RESOLVE_TECHS`、`REDLINES/TRAJECTORY_*/CASES`(可疑分析数据)、`COPY` |
| `src/llm.js` | `/api/llm` 封装 + 离线兜底:`askVictim/analyzeText/healReply`、可疑分析三层引擎(`scanRedlines/analyzeSuspicious/ocrImage/trajectoryVerdict`)、`detectCrisis`(危机词硬规则) |
| `src/Lens.jsx` | 可疑分析:粘文字/传截图(OCR)/关系体检三 tab + 判读卡 + 案例库 |
| `src/Heal.jsx` | 心理疗愈:分流(刚玩完/真受害者)+ 情绪命名 + AI陪伴 + **危机卡** + 恢复联结 + 热线 |
| `api/llm.js` | Vercel serverless 代理(生产用) |
| `vite.config.js` | dev 代理 + 条件 base |
| `玩法-VN混合-spec.md` | VN 玩法的完整规格(改玩法先读它) |

---

## 5. 审计结果:已修 / 待办

本会话跑了 8 维度多智能体审计(27 条已验证)。

**✅ 已修并上线(6 高危 + 数条中危)**
- base 路径白屏(Pages/Vercel 双兼容)
- 危机词大量漏网(扩词 + 去空格归一化 + 英文兜底 + 危机模式持续锁定)
- 红线误报:"出国留学/日结/投资" 被误判缅北极高危 → 收敛关键词
- Lens `.tech-counter` 样式冲突(判读反制文字改 `.verdict-counter`)
- 生产 `/api/llm` 404 → Vercel 函数
- investor h3 选项 quality 与 RESOLVE_TECHS 对齐(原来教学教反)
- 案例点击直接展示自带判读(离线也能完整演示)

**🟠 待办(medium/low,不阻断上线,建议继续清)**
- Lens 切标签时上一 tab 的在途分析结果会串台渲染 → 加 AbortController / mode 守卫(`Lens.jsx`)
- guard 起疑弹窗关闭后原地重放同一 hinge:玩家上一句没回应、`movesRef` 重复记账、`objRef` 被强制清除(`Scammer.jsx` triggerGuard/dismissGuard)
- 离线(无 key)时:自由输入被判"无"仍加信任显示"到点了"(教学信号反转);`offlineVerdict` 无红线时给"低风险·暂时安全"误导 → 改中性文案
- hinge 选 jump 时作者写的「（h 急）」台词不显示(`getVictimReply` 在 replyKind='susp' 提前 return)
- 两处 WCAG 对比度不足:`--text-secondary`(#6B7169)在纸底约 3.8:1;meter/stage-hint(#8a8378)约 3.5:1 → 加深
- 死数据清理:`COPY.care.hotlines` 的 12355 → 12356;`stageAtLeast` 字段没被读取;`恐惧驱动` 是第 9 招但首页写"8种技法"口径不一;首页"损失追回率 4%"无出处

---

## 6. 怎么跑 / 怎么部署

- **本地**:`npm install` → `npm run dev` → http://localhost:5174 (需要本地 `.env` 里有 `HJ_LLM_KEY` 才有实时 AI;没有也能跑,走离线兜底)
- **构建**:`npm run build` → `dist/`
- **部署**:`git push origin main` → Vercel 自动构建部署。key 在 Vercel `Settings → Environment Variables → HJ_LLM_KEY`,改 key 后需 Redeploy。

## 7. 已知坑

- 工作目录是 `E:\trae ai\怀瑾`,别去动 `C:\Users\JYQ74\huaijin` 旧副本。
- Play 里所有异步(advance/hinge/win/lose)以 `cursorRef/trustRef/suspRef/aliveRef` 为真源,别用对应的 state 做异步判断(StrictMode 会 stale/卡死)。
- 反转的杀伤力来自"复述玩家亲手打的话"——改玩法时别让 hinge 之外的自动台词冒充玩家原话。
- 密钥永远不进仓库/聊天/前端。
