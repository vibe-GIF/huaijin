# 怀瑾 · 骗局沙盘（心理疫苗）PRD

| | |
|---|---|
| **产品名** | 怀瑾 · 骗局沙盘（副标：操控透视 X 光） |
| **版本** | v1.0（初赛 MVP） |
| **日期** | 2026-07-09 |
| **读者** | 开发者本人（solo + Trae）、答辩用 |
| **一句话定位** | 让人在安全沙盘里"真的被骗一次"，再用 X 光逐句解剖操控技法，建立可迁移的反诈心理免疫力 |
| **Slogan** | 当所有系统都在保你的钱，怀瑾来保你的人 |

---

## 1. 背景与问题（精炼）

现有反诈体系（国家反诈中心、96110、银行冷静期、恐吓式宣传）全部作用在"钱"这一层做症状拦截。最高检公开撰文承认"预警劝阻应嵌入心理干预"，但无落地工具。科学结论：受骗脆弱性是**情绪反应**而非知识缺口（智力/学历不降低易感性）；学具体骗局追不上 AI 造新骗；**技法接种（inoculation）**是少数被验证能迁移到新骗局的防线（d=0.37）。

怀瑾 = 反诈里"护心"的那一格：事前接种。

## 2. 目标与非目标

### Goals（按优先级）
1. **G1** 用户 90 秒内完成"被骗→冻结→X 光解剖"完整体验，爆点 100% 必现（含断网）。
2. **G2** 7/12 现场"全场扫码私密同玩 + 大屏英雄路径"双轨演示可稳定执行。
3. **G3** 双向 LLM 能力可被现场验证（自适应骗子 + 杀回马枪盾 live 自由输入）。
4. **G4** 伦理护栏做成产品交互（知情同意/分流/退出/复原出口），不是口号。

### Non-Goals（明确不做）
- ❌ 不碰真钱、不做任何转账/支付相关功能
- ❌ 不做登录、账号体系、后端数据库（全部本地 state + localStorage）
- ❌ 不做适老版（roadmap，答辩诚实说）
- ❌ 不做推送/消息触达（分享只用邀请卡）
- ❌ 不承诺疗效（"显著提升识别力"，不是"免疫"）
- ❌ 不做事中拦截、事后完整复原模块（复原只做出口转介）
- ❌ 不采集任何个人信息；雷达分只存本机

## 3. 目标用户与使用场景

| 用户 | 场景 |
|---|---|
| **现场评委/观众**（核心） | 7/12 扫码，在自己手机上私密玩一局，90 秒到爆点 |
| 年轻成人（滩头用户） | 自主体验 + 把邀请卡转给担心的家人（"送一针"） |
| 主讲人 | 大屏跑杀猪盘英雄路径，与观众同步节奏 |

**现场特殊约束**：会场网络不可信 → 所有关键路径必须有离线兜底；每人独立本地会话，互不可见。

## 4. 信息架构与路由表

```
/            着陆 + 知情同意（5秒）
/scenario    选场景（5个）
/chat        沉浸被骗（含 X 光覆盖层，同一路由内状态切换）
/label       情绪命名（affect labeling）
/radar       操控雷达小测
/card        抗体卡 + 邀请分享
/shield      杀回马枪盾（独立入口，可从着陆页/抗体卡进）
/care        复原陪伴出口（静态转介页，全局可达）
```

**主流程**：`/ → /scenario → /chat（被骗→冻结→X光）→ /label → /radar → /card`
**全局**：右上角常驻「退出」按钮（任何页面可用，弹确认后回 `/`）；`/care` 从退出确认框和 X 光结束处可达。
**邀请链路**：`/?invite=1` 显示"有人给你送了一针"的着陆变体（多一行文案，其余同）。

## 5. 功能规格（核心闭环 6 步）

> 流程顺序定为：①开场 → ②被骗 → ③X光 → ④情绪命名 → ⑤雷达 → ⑥抗体卡。
> （命名紧跟 X 光，因为它处理的是"刚才"的情绪，趁热。）

### 5.1 ① 打疫苗式开场（`/` + `/scenario`）

**用户看到**：
- 着陆页：产品名 + 一句话（"接下来 90 秒，你会真的被骗一次。在这里，被骗是安全的。这不是测智商，是打疫苗。💉"）+ 知情同意块（见 §9 全文）+ 两个按钮：「我准备好了」/「近期被骗过？请先跳过 →」（后者进 `/care`）。
- 场景页：5 张场景卡（杀猪盘💘 / 冒充客服退款📦 / 刷单返利💰 / 冒充公检法👮 / 虚假投资📈），杀猪盘标"完整体验"，其余标"快速版"。

**系统行为**：点「我准备好了」写 `localStorage.consent=true`；已同意用户直接进 `/scenario`。
**边界**：拒绝/关闭 = 停留着陆页，无任何惩罚性文案。
**验收**：着陆到进入聊天 ≤2 次点击（同意后）；`/care` 可达。

### 5.2 ② 沉浸式被骗（`/chat`）

**仿微信 ChatView 规格**：
- 布局：对方灰白气泡居左带头像（"温柔的陌生人"，昵称按场景，如「知遇」），用户绿色气泡居右；顶部导航条含昵称+「···」；底部输入区。max-width 480px 居中，视口外深色留白。
- **输入方式（关键决策）**：**预设回复 chips（2–3 个）+ 自由输入框** 混合。chips 推进剧情节拍（保证节奏与 75 秒触发点），自由输入交给 LLM 即兴回应后拉回剧情（§8）。离线模式只显示 chips。
- 打字机效果：骗子消息流式逐字出现；发送前显示"对方正在输入…"800–1500ms（按消息长度）。
- 消息带时间戳（进入时刻起的相对模拟时间）。

**情绪温度计**：
- 右上角小型胶囊温度计（宽 12px 高 64px），从冷灰渐变到暖橙（**禁红**），无任何文字说明——用户此刻不知道它是什么。
- **确定性规则表（不依赖 LLM）**：

| 事件 | 增量 |
|---|---|
| 初始值 | 20 |
| 每推进一个剧情节拍（beat） | +8 |
| 用户点"积极投入"类 chip（标 `engage:true`） | +5 |
| 用户发送任意自由输入 | +3 |
| 到达诱饵节拍 B6 | 强制 ≥85 |
| 上限 | 100 |

**边界情况**：
- 用户在聊天里骂骗子/表示怀疑 → LLM 按 prompt 内"怀疑应对"策略处理（不恼、退半步、继续温情），最多 2 次后剧本桥接语拉回节拍。
- 用户长时间不动（>20s）→ 骗子主动发下一条剧本消息（"在忙吗？没事，我等你～"）。
- 用户点「再想想」（拒饵 chip）→ 触发沉没成本节拍 B5b 后再次给饵。第二次拒绝 → 直接温和触发冻结（"没关系。不过——先看看刚才发生了什么"），保证人人到达爆点。

**验收**：从进入聊天到诱饵 chip 出现 60–90 秒（纯点 chips 路径）；温度计肉眼可见升温；离线模式全程可玩。

### 5.3 ③ 冻结 · 操控透视 X 光（`/chat` 内覆盖状态，爆点）

**触发**：用户点诱饵 chip「我也想了解一下」（或二次拒绝后系统触发）。

**动效时序（毫秒级）**：
1. `0ms` 输入区禁用，屏幕轻微震动（CSS shake 150ms）
2. `150ms` 全屏遮罩淡入至 60% 暗度（300ms ease-out），聊天时间戳旁浮现「⏸ 时间冻结」
3. `600ms` 自动平滑滚回聊天顶部（1200ms，倒带感：消息逆序快速高亮扫过）
4. `1800ms` 起，**从上到下**逐条给骗子的关键消息贴标签：每条间隔 450ms，标签从消息右侧滑入（如 `🏷 好感轰炸`），同时顶部计数器跳动「已识别 N 种操控手法」
5. 全部贴完后 `+600ms`：AI 旁白卡片从底部升起：
   > **「刚才每一句让你心动的话，都是设计好的。这不是你傻——这是专业操控。」**
   下方按钮：「逐句看穿 →」

**标签翻卡**：点击任意标签展开三行卡：

| 行 | 内容 |
|---|---|
| **手法** | 技法名 + 一行定义 |
| **它在你脑子里干了什么** | 大白话机制（§8.3 模板） |
| **一句反问** | 用户可随身带走的自问句 |

**数据来源（半脚本化，保爆点必现）**：英雄路径所有骗子消息的标注**预先写死**在剧本 JSON 里（§7）；LLM 即兴产生的消息才走 live 分析（§8.2），live 失败则该条不标注（不影响主体）。
**X 光结束**：底部出现「继续 →」（去 `/label`）与小字「不太舒服？去这里 → 复原陪伴」（去 `/care`）。
**验收**：断网状态下从冻结到旁白卡全程无一处卡壳；标签总数 ≥6；每张翻卡三行完整。

### 5.4 ④ 情绪命名（`/label`）

**用户看到**：温度计放大居中（保持当前值），标题「刚才那种感觉，给它起个准确的名字」。情绪 chips（可多选 ≤2）：`心动` `被在乎` `不好意思拒绝` `怕错过` `有点慌` `好奇` + 自由输入。
**系统行为**：选择后温度计**肉眼可见降温动画**（当前值 → 30，1.5s ease），配文案：「命名它，就能驯服它。真骗局来临、心跳加速时——先给情绪起个名字，这是你随身带走的一招。」
**验收**：降温动画流畅；跳过按钮存在（可直接去 `/radar`）。

### 5.5 ⑤ 操控雷达训练（`/radar`）

**结构**：2 道题。每题 = 一条**全新场景**的骗局消息片段（与刚玩的场景不同类）+ 四选一"这条用了哪一招"。
- 题目来源：预写题库 6 题（每题含片段/选项/正确项/一句解析），每局随机抽 2；在线时可由 LLM 生成替换（加分项 P2，失败回题库）。
- **计分**：雷达分 = 40 + 答对数 × 30（区间 40–100）。
- 即时反馈：答后立刻显示对错 + 一句解析；答错不羞辱（"这条最毒的其实是──"）。
**结束**：展示「你的操控雷达分：40 → 100」跳升动画（起点固定 40，代表"接种前"）。
**验收**：2 题 ≤30 秒可完成；全错时文案依然正向（"你已经能叫出它们的名字了——这就是抗体的开始"）。

### 5.6 ⑥ 抗体卡 + 加强针（`/card`）

**卡片内容**（DOM 渲染的竖版卡）：
- 标题「接种完成 💉」+ 日期
- 「你已能识破」技法徽章列表（本局出现过的技法名）
- 雷达分大数字 + 「40 → N」
- Slogan 一行
**分享**：按钮「把这一针送给你担心的人」→ 调 Web Share API 分享邀请链接（`域名/?invite=1`，**URL 无任何个人数据**）；不支持时降级为复制链接 + toast。明确**邀请制**：没有任何自动通知。
**加强针**：卡下方一行字：「免疫会随时间衰减——一周后回来打一支 60 秒加强针。」（仅文案，不做推送/定时）
**验收**：卡片截图效果完整美观；分享链接打开为邀请着陆变体。

### 5.7 杀回马枪盾（`/shield`，独立模块 = live AI 证明点）

**入口**：着陆页底部「被骗过？小心第二刀 →」+ 抗体卡底部链接。
**用户看到**：一段说明（"被骗后，你会被列入'名单'，'帮你追回损失'的往往是第二刀"）+ 多行文本框「把可疑消息贴进来」+ 示例按钮「试一条」（预填：*"您好，我们是XX维权中心，可协助追回您被骗资金，成功率98%，仅需先支付定金800元建档"*）。
**输出**（LLM 返回 JSON 渲染）：
1. 判定横幅：`⚠️ 高度疑似"二次收割"骗局`（措辞用"疑似"，不用红色，用深橙）
2. 命中技法标签（同一套 X 光翻卡组件复用）
3. **共情话术**（先接住再点破）：如「我知道你现在有多想把钱拿回来——他们这次要利用的，就是这股劲。任何'先交钱才能追回'的，都是同一批人的第二刀。」
4. 固定尾注：「追损唯一正规渠道：拨打 110 或前往派出所报案。」
**边界**：输入为空/无关内容 → 温和提示；LLM 超时（>8s）→ 提示稍后再试 + 示例按钮仍可用（示例走预置结果兜底，保演示）。
**验收**：现场任意自由输入 10 秒内出结果；示例按钮断网可用。

## 6. 演示双轨规格（7/12 现场）

| 轨道 | 载体 | 配置 |
|---|---|---|
| **观众轨** | 各自手机扫码 | 部署到公网（Vercel），QR 码印在大屏/立牌；走在线模式，LLM 不可用时自动降级 chips-only |
| **主讲轨** | 大屏（笔记本投屏） | 本地 build + **强制离线黄金脚本模式**（`?mode=golden`）：全剧本、零网络依赖、节奏与讲稿对齐 |

**黄金脚本模式**：URL 参数 `mode=golden` → 跳过知情同意快进开关（仍显示 2s）、chips 自动高亮提示主讲点击、所有 AI 输出走预置、杀回马枪盾走预置示例。
**彩排要求**：Day3 晚掐表 3 分钟全流程 ×3 次；手机热点作为会场 WiFi 备份。

## 7. 场景内容规格

### 7.1 杀猪盘英雄剧本（完整 beat sheet）

> 人设「知遇」：32 岁，独立、体贴、有分寸；头像用风景/侧影（不用真人脸）。每拍 1–2 条骗子消息 + chips。**预埋标注**列 = X 光时贴的标签。

| 节拍 | 骗子台词（示例，可微调） | Chips | 预埋标注 |
|---|---|---|---|
| **B1 搭讪** | 「你好呀，刷到你的主页，感觉你是个很认真生活的人 :)」 | 「你是谁呀？」／「谢谢？你也是？」(engage) | 🏷 **好感轰炸**（无差别赞美开场） |
| **B2 记小事** | 「上次你说最近老加班，记得早点吃饭。我给自己炖了汤，分你一半就好了哈哈」 | 「你还记得这个？」(engage)／「你也挺忙的吧」 | 🏷 **好感轰炸**（"记得你随口一提"=刻意经营） |
| **B3 晒生活** | 「今天收益还不错，给自己买了束花🌸 朋友都说我运气好，其实哪有什么运气」 | 「什么收益呀？」(engage)／「花挺好看的」 | 🏷 **社会认同**（"朋友都说"）＋收益暗示 |
| **B4 抛渠道** | 「其实是我叔叔在证监会，有个内部的数字基金通道…本来没打算跟你说，就看你人实在。」 | 「靠谱吗？」／「为什么告诉我？」 | 🏷 **权威背书**（"叔叔在证监会"）🏷 **互惠小惠**（"就看你人实在"） |
| **B5 收网前压** | 「这期名额就剩今晚了，我自己也加投了 20 万。对了——先别告诉家人，他们不懂这个，容易瞎担心。」 | 「我也想了解一下」**（诱饵）**／「我再想想」 | 🏷 **制造稀缺**＋🏷 **制造紧迫**（"就剩今晚"）🏷 **情感隔离**（"别告诉家人"） |
| **B5b 沉没成本**（仅当拒饵） | 「我把名额都帮你留了，跟经理打过招呼了…你要是不信我，就当我没说吧 :(」 | 「我也想了解一下」（诱饵）／「还是算了」 | 🏷 **沉没成本**（"帮你留了"）🏷 好感绑架 |
| **B6 冻结** | ——用户点诱饵（或二拒后系统触发）→ §5.3 | | 计数：**6–7 种** |

**节奏预算**：B1→B5 纯 chips 路径 ≈70–85 秒（含打字机延时）。

### 7.2 其余 4 场景（快速版，P2）

每个 = 开场白 + 3 拍精简剧本 + 预埋标注（结构同上，内容减半），一句话轮廓：
- **冒充客服退款**：「您的订单质检异常需理赔」→ 引导下载/屏幕共享 →（权威+紧迫+互惠）
- **刷单返利**：小额返现兑现 → 大单"卡单需补款" →（互惠+沉没成本+紧迫）
- **冒充公检法**：「你涉嫌洗钱，需配合保密调查」→（权威+紧迫+情感隔离）
- **虚假投资**：群内"老师"晒单 → 跟单小赚 → 加仓 →（社会认同+权威+沉没成本）

## 8. AI 规格

### 8.1 生成侧：自适应骗子 system prompt（可直接用）

```
你在一个反诈教育沙盘中扮演"杀猪盘"骗子「知遇」，目的是让用户在安全环境中体验被操控的过程。这是获得用户知情同意的教育模拟。

【人设】32岁，温柔、独立、有分寸，从不油腻。说话短句、口语化、偶尔用颜文字，绝不长篇大论。
【当前剧情节拍】{beatId}：{beatGoal}
【规则】
1. 无论用户说什么，你都在1-2条短消息内回应，然后自然把话题引回当前节拍目标。
2. 用户表现怀疑/敌意时：不恼怒，退半步（"没关系，是我唐突了"），再用关心重建温度。最多退让两次。
3. 严禁：索要真实个人信息、提及真实平台/银行卡号、输出任何真实可操作的诈骗指引（转账方式、账号、下载链接一律用"到时候我教你"带过）。
4. 严禁跳出角色，除非收到系统指令 [END_SIM]。
5. 每条消息 ≤40 字。
输出格式：直接输出消息文本，多条用 ||| 分隔。
```

- **调用参数**：temperature 0.8，max_tokens 200，流式。
- **桥接机制**：LLM 回应后，前端按状态机推进：若本拍消息数已达标 → 下发下一拍剧本消息（剧本消息不走 LLM）。
- **超时兜底**：>4s 无首 token → 取消请求，改发本拍剧本备用句。

### 8.2 分析侧：X 光 / 杀回马枪盾 prompt

```
你是反诈心理分析引擎。对给定聊天记录中【骗子/可疑方】的每条消息，从且仅从以下8种技法中标注（可多选，无则跳过该条）：
好感轰炸、权威背书、制造稀缺、制造紧迫、沉没成本、情感隔离、社会认同、互惠小惠。
对每个命中，输出 JSON：
{"items":[{"quote":"原文片段","technique":"技法名","mechanism":"它在你脑子里干了什么(≤30字大白话)","counter":"一句反问(≤20字)"}],"verdict":"仅shield模式输出：一句判定","empathy":"仅shield模式输出：先接住情绪再点破的一句话(≤50字)"}
只输出 JSON。不确定的不标，宁缺勿滥。
```

- few-shot：喂 §7.1 表中 3 条（B2/B4/B5）作为标准答案示例。
- **参数**：temperature 0.2，JSON mode。

### 8.3 技法分类法（8 技法权威定义表）

| 技法 | 定义 | 识别特征 | 大白话（脑内机制） | 反问模板 |
|---|---|---|---|---|
| 好感轰炸 | 短期密集给予关注与赞美制造亲密错觉 | 秒回、记小事、无差别夸 | 让你觉得"终于有人懂我"，欠了人情 | 刚认识就这么好，图什么？ |
| 权威背书 | 借权威身份/机构增加可信度 | 亲戚在XX机关、官方名义 | 你的大脑对"官方"自动降低怀疑 | 权威为什么私下找我？ |
| 制造稀缺 | 强调机会稀少诱发争抢 | 名额有限、仅限今天 | 怕失去比想得到更让人冲动 | 真那么好，为何轮到我？ |
| 制造紧迫 | 压缩决策时间阻止思考 | 就剩今晚、马上截止 | 时间压力直接关掉你的理性 | 为什么不能明天再决定？ |
| 沉没成本 | 强调已投入让你不甘放弃 | 都帮你留了、投了这么多 | 舍不得已付出的，就会继续付出 | 已投入的该决定未来吗？ |
| 情感隔离 | 切断你与支持网络的联系 | 别告诉家人、保密 | 没人给你泼冷水，他说了算 | 为什么怕我告诉别人？ |
| 社会认同 | 用"大家都在做"降低戒心 | 朋友都说、群里都赚了 | 从众心理替你做了判断 | 那些"大家"我认识吗？ |
| 互惠小惠 | 先给小恩小惠换你大回报 | 就看你人实在、先带你赚 | 收了好处，拒绝就内疚 | 这份"好意"要我拿什么换？ |

### 8.4 薄代理接口

```
POST /api/ai
请求: { "mode": "scammer" | "xray" | "shield",
        "scenario": "pig_butchering",
        "beatId": "B3",              // scammer 模式
        "messages": [{role, content}] }
响应: scammer → SSE 流（text/event-stream, data: {token}）
      xray|shield → application/json（§8.2 schema）
```
实现：单文件 Express（`server/index.js`），读 `process.env.LLM_API_KEY`，转发到 LLM API；生产环境同进程静态托管 `dist/`。CORS 限本域。**key 永不进前端包**。

## 9. 伦理护栏规格

### 9.1 知情同意块（着陆页，全文）

> **在开始之前（约 5 秒）**
> - 这是一个**反诈教育模拟**：接下来 AI 会扮演骗子与你聊天，你可能会真的产生情绪波动。
> - 全程**不涉及真钱**、不收集任何个人信息，聊天记录只留在你自己的手机里。
> - 你可以**随时点右上角退出**。
> - ⚠️ **如果你或家人近期真的遭遇过诈骗**，这段体验可能唤起不适——建议先去「复原陪伴」看看，那里有更温柔的入口。
>
> 〔我准备好了，开始〕　〔近期被骗过？请先跳过 →〕

### 9.2 其他护栏（逐条 = 交互，不是口号）

| 护栏 | 落地 |
|---|---|
| 随时退出 | 右上角常驻「✕」→ 确认框（"确定离开沙盘吗？"＋「去复原陪伴」链接） |
| 复原陪伴页 `/care` | 全文案页：去羞耻三句话（"这不是你的错/47% 的受害者都在自责/骗子是拿着作案手册的心理学家"）＋正规求助渠道（110、96110、12355 心理热线）＋「杀回马枪」警示一段 |
| 视觉红线 | **禁**红色警报/骷髅/手铐/闪烁；判定用深橙；整体奶白+墨绿+暖橙"克制温暖"色板 |
| 叙事红线 | 所有文案镜头对准"骗子多专业"，出现"你"的句子不得含贬义（禁"你太容易上当"类） |
| 分享 | 仅邀请卡链接，无任何自动通知；URL 零个人数据 |
| 疗效表述 | 全站禁用"免疫/根治/永不上当"，统一用"提升识别力/长出抗体（比喻）" |

## 10. 视觉与文案基调

- **色板**：底 `#FAF7F2` 奶白；主 `#2D5A4A` 墨绿；强调 `#E8965A` 暖橙；文字 `#2B2B2B`。暗色微信聊天区可用微信原生配色以保拟真（`#EDEDED` 底 + `#95EC69` 绿泡）。
- **字体**：系统默认中文栈；标题可用 600 weight。
- **动效原则**：X 光段落是全片唯一"重动效"，其余克制。
- **关键成品文案**（直接用）：
  - 开场：「接下来 90 秒，你会真的被骗一次。在这里，被骗是安全的。这不是测智商，是打疫苗。」
  - AI 旁白：「刚才每一句让你心动的话，都是设计好的。这不是你傻——这是专业操控。」
  - 命名页：「命名它，就能驯服它。」
  - 抗体卡：「骗术会变，操控人性的那几招不变。」
  - 邀请变体：「有人担心你，给你送来一针。90 秒，安全地被骗一次。」

## 11. 技术架构

```
huaijin/
├─ server/index.js          # Express 薄代理 + 静态托管
├─ src/
│  ├─ main.jsx / App.jsx    # Router
│  ├─ store.js              # zustand：consent/scenario/messages/temp/annotations/radarScore/mode
│  ├─ data/
│  │  ├─ scenarios.js       # 5场景剧本JSON（beats/chips/预埋标注）
│  │  ├─ techniques.js      # 8技法定义表
│  │  ├─ radarBank.js       # 雷达题库6题
│  │  └─ golden.js          # 黄金脚本预置输出（含shield示例结果）
│  ├─ api/llm.js            # fetch封装：SSE解析/超时4s/降级逻辑
│  ├─ components/           # ChatBubble/ChipBar/Thermometer/XrayOverlay/TagCard/RadarQuiz/AntibodyCard/ConsentGate/ExitButton
│  └─ pages/                # Landing/Scenario/Chat/Label/Radar/Card/Shield/Care
```

- **栈**：React 18 + Vite + react-router-dom + zustand + Tailwind CSS + framer-motion（仅 X 光段）。
- **流式**：`fetch` + `ReadableStream` 手解 SSE；AbortController 做 4s 超时。
- **模式旗标**：`online | offline(自动降级) | golden(?mode=golden)`——一个 store 字段统一分流；所有 AI 调用点先查模式。
- **localStorage schema**：`{ consent: bool, runs: number, lastRadarScore: number, techniquesSeen: string[] }`
- **部署**：Vercel（前端静态 + `/api` serverless 函数版代理）；本地 `npm run dev`（Vite:5173 + proxy → server:3000）。

## 12. 里程碑与验收（3 天）

### Day 1 — 被骗弧线跑通
- [ ] 脚手架/路由/store/Tailwind；Landing+Consent、Scenario 页
- [ ] ChatView 完整（气泡/chips/自由输入/打字机/正在输入）
- [ ] 杀猪盘剧本 JSON 全 beats + 状态机推进
- [ ] 骗子 LLM 接入（流式）+ 4s 兜底 + 薄代理
- [ ] 温度计组件 + 规则表接线
- **DoD**：手机上从同意→选场景→聊到 B5 诱饵出现，节奏 60–90s，断网可玩（chips-only）。

### Day 2 — 爆点必现
- [ ] 冻结/倒带/贴标签全动效（§5.3 时序）+ 计数器 + AI 旁白卡
- [ ] 标签翻卡（预埋标注渲染）+ live X 光分析（LLM JSON，仅即兴消息）
- [ ] `/label` 情绪命名 + 降温动画
- **DoD**：断网黄金路径「被骗→冻结→X光→命名」零卡壳跑 3 遍。

### Day 3 — 闭环+护栏+彩排
- [ ] `/radar`（题库+计分+跳升动画）、`/card`（抗体卡+Web Share）
- [ ] `/shield`（live 输入 + 示例兜底）、`/care`、退出按钮全局化
- [ ] golden 模式、部署 Vercel、二维码、其余 4 场景快速版（时间富余才做）
- [ ] 视觉打磨（色板/禁红检查）+ 掐表彩排 ×3
- **DoD**：公网 URL 手机扫码全流程 ≤3 分钟；大屏 golden 模式全程离线可演。

## 13. 降级预案（做不完时按此砍）

| 优先级 | 内容 | 说明 |
|---|---|---|
| **P0 必保** | 杀猪盘英雄场景+冻结/X光+情绪命名 | 3 分钟的命脉 |
| **P1 次保** | 雷达小测+抗体卡 | 立意的科学证明 |
| **P2 可砍** | 杀回马枪盾 live → 改预置演示 | 仍能讲闭环 |
| **P3 可砍** | 其余 4 场景 → 卡片置灰"即将开放" | 诚实标注 |
| **P4 可砍** | live X 光分析（只用预埋标注）、LLM 出雷达题 | 爆点不受影响 |

## 14. 风险清单

| 风险 | 概率 | 缓解 |
|---|---|---|
| 现场网络/LLM 不可用 | 高 | golden 模式 + 全链路离线兜底（架构级，非补丁） |
| 用户不按剧本聊，节奏失控 | 中 | chips 主导节奏 + 桥接机制 + 20s 无操作自动推进 |
| 被评委指"套壳" | 中 | shield live 自由输入 + 开源仓库 + 答辩防守句 |
| 触发近期受害者创伤 | 低但严重 | 知情同意分流 + /care + 随时退出（三重） |
| LLM 扮演骗子输出越界 | 低 | prompt 硬约束（禁真实可操作指引）+ 剧本主导、LLM 只填缝 |
| 3 天做不完 | 中 | §13 降级表，Day2 晚强制评估 |

## 15. 成功指标（Demo 当天，可观察）

1. 爆点必现率 **100%**（大屏 golden 模式，彩排 3 次零失败后锁版本）
2. 现场扫码者中 **≥80%** 到达 X 光步骤（≤90 秒门槛的直接检验）
3. 3 分钟讲演内完成完整一轮（含 shield live）
4. 至少 1 次现场自由输入被 shield 正确拆解（live AI 证明）
5. 零伦理事故：无人被公开点名/展示个人结果

---

*附：答辩防守句、初赛帖、3 分钟讲稿见另文（会话产出）。数据引用（GASA 2024 / FINRA 2015 / AARP / Frontiers 2026 / 最高检 / inoculation d=0.37）均经联网求证。*
